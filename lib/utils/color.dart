import 'package:flutter/material.dart';

class ThemeColors {
  static const splashScreenTheme = Colors.white;
  static const seedColorTheme = Colors.purple;
  static const backgroundNavbarTheme = Colors.purple;
  static const colorTextNavbarTheme = Colors.white;
  static const unselectedNavbarItem = Color.fromARGB(255, 224, 0, 227);
  static const selectedNavbarItem = Color.fromARGB(255, 82, 0, 76);
  static const cardTextColor = Colors.black;
  static const cardBackgroundColor = Colors.white;
}
